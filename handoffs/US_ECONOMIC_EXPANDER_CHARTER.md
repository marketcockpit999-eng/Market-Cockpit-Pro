# 📈 US Economic Data Expander化 作業憲章 v2.0
# ============================================

> **⚠️ 強化版作業憲章**
> 前回のMOBILE_SCROLL_FIX_CHARTERで100%遵守されなかった点を改善。
> より厳格なルールと明確なチェックポイントを設定。

---

## 🎯 目的

`pages/03_us_economic.py`のセクションを`st.expander`で折りたたみ可能にする。
モバイルでのスクロール問題を解消し、ユーザー体験を向上させる。

---

## 📊 ファイル情報

| 項目 | 値 |
|------|-----|
| ファイル | `pages/03_us_economic.py` |
| サイズ | 約460行 |
| セクション数 | 8つ |
| 難易度 | ⭐⭐⭐（Employment部分が複雑） |

---

## ⛔ 絶対禁止事項（違反 = 即ロールバック）

### 🚨 CRITICAL: これを破ったら作業停止

```
❌ show_metric_with_sparkline の引数変更
❌ display_macro_card の引数変更
❌ styled_line_chart の引数変更
❌ get_richmond_fed_survey() 呼び出しの変更
❌ get_richmond_fed_services_survey() 呼び出しの変更
❌ st.metric の引数変更
❌ st.plotly_chart の引数変更
❌ if文の条件式変更
❌ 変数名の変更
❌ import文の変更
❌ 関数定義の変更（show_date_info）
❌ 複数セクションを1回の編集で変更
❌ 「ついでに」の改善
```

### 🔒 違反時のロールバック手順

```bash
# 1. 変更を破棄
git checkout -- pages/03_us_economic.py

# 2. 状態確認
git status

# 3. 最初からやり直し
```

---

## ✅ 許可されている作業（これだけ）

```
✅ st.markdown(f"### {t('...')}") を削除
✅ with st.expander(t('...'), expanded=True): を追加
✅ 既存コードのインデントを1レベル（4スペース）追加
✅ st.markdown("---") はexpander外に残す
```

---

## 📋 セクション分割計画（5スレッド）

| Phase | セクション | 行番号（目安） | 作業量 | 状態 |
|-------|-----------|---------------|--------|------|
| 5-1 | Employment（雇用） | 57-193 | 大（130行） | ⬜ |
| 5-2 | Inflation + Economy | 195-273 | 中（75行） | ⬜ |
| 5-3 | Michigan Inflation + Leading | 275-295, 497-515 | 小（40行） | ⬜ |
| 5-4 | Manufacturing | 297-395 | 中（100行） | ⬜ |
| 5-5 | Services + Housing | 397-495, 517-542 | 中（120行） | ⬜ |

### スレッド作業量の根拠
- **5-1**: Employment は内部に複数の `st.markdown("---")` があり、最も複雑
- **5-2**: Inflation/Economy は `display_macro_card` 使用でシンプル
- **5-3**: 小さいセクション2つを統合
- **5-4**: Manufacturing は Richmond Fed の特殊処理がある
- **5-5**: Services は Manufacturing と同構造 + Housing追加

---

## 🔧 変更パターン（厳密に従うこと）

### パターンA: 標準セクション

**Before:**
```python
# === 2. Inflation ===
st.markdown("---")
st.markdown(f"### {t('us_economic_section_inflation')}")
col1, col2 = st.columns(2)

with col1:
    display_macro_card(...)
```

**After:**
```python
# === 2. Inflation ===
st.markdown("---")
with st.expander(t('us_economic_section_inflation'), expanded=True):
    col1, col2 = st.columns(2)

    with col1:
        display_macro_card(...)
```

### パターンB: captionがある場合

**Before:**
```python
st.markdown("---")
st.markdown(f"### {t('us_economic_section_manufacturing')}")
st.caption(t('manufacturing_indicators_desc'))

mfg_col1, mfg_col2 = st.columns(2)
```

**After:**
```python
st.markdown("---")
with st.expander(t('us_economic_section_manufacturing'), expanded=True):
    st.caption(t('manufacturing_indicators_desc'))

    mfg_col1, mfg_col2 = st.columns(2)
```

### パターンC: すでに内部expanderがある場合（Manufacturing/Services）

**重要**: 内部の `with st.expander(t('manufacturing_guide')...` はそのまま維持

**Before:**
```python
st.markdown("---")
st.markdown(f"### {t('us_economic_section_manufacturing')}")
...
with st.expander(t('manufacturing_guide').split('\n')[0], expanded=False):
    st.markdown(t('manufacturing_guide'))
```

**After:**
```python
st.markdown("---")
with st.expander(t('us_economic_section_manufacturing'), expanded=True):
    ...
    # 内部expanderはそのまま！
    with st.expander(t('manufacturing_guide').split('\n')[0], expanded=False):
        st.markdown(t('manufacturing_guide'))
```

---

## 📝 スレッド開始時の必須宣言

**コピペ必須。省略禁止。**

```markdown
## 開始宣言

**必読ドキュメント確認**:
- [x] US_ECONOMIC_EXPANDER_CHARTER.md を読みました
- [x] 絶対禁止事項を理解しました
- [x] 変更パターンを確認しました

**このスレッドの作業**:
- Phase: 5-[X]
- 対象セクション: [セクション名]
- 行番号: [開始行]-[終了行]

**やること**:
1. [具体的な変更1]
2. [具体的な変更2]

**やらないこと**:
- 他のセクションの変更
- show_metric_with_sparkline等の引数変更
- 「ついでに」の改善

**ロールバック準備**:
git checkout -- pages/03_us_economic.py
```

---

## ✔️ 変更前チェックリスト

**全項目確認必須。1つでも未確認なら作業開始禁止。**

```markdown
## 変更前チェック

- [ ] git status で未コミット変更がないことを確認
- [ ] 現在のファイルをエディタで開いて対象行を確認
- [ ] 変更対象セクションの開始行と終了行を特定
- [ ] 変更パターン（A/B/C）を特定
- [ ] 内部にst.expanderが既存するか確認
- [ ] 「ロールバックコマンド」を手元に控えた
```

---

## ✔️ 変更後チェックリスト

**全項目確認必須。1つでも失敗なら即ロールバック。**

```markdown
## 変更後チェック

### 構文チェック
- [ ] `streamlit run market_app_nav.py` でエラーなく起動
- [ ] 📈 US Economic Data タブが開ける

### 表示チェック
- [ ] 対象セクションのexpanderが表示される
- [ ] expander内のデータが正常に表示される
- [ ] expanderの折りたたみ/展開が動作する
- [ ] 他のセクションに影響がない

### データチェック
- [ ] show_metric_with_sparkline の値が変化していない
- [ ] チャートが正常に描画される
- [ ] st.metric の値が変化していない

### 失敗時
git checkout -- pages/03_us_economic.py
```

---

## 📝 スレッド終了時の必須報告

**コピペ必須。省略禁止。**

```markdown
## 完了報告

### 作業内容
- Phase: 5-[X]
- 対象: [セクション名]
- 変更行: [開始行]-[終了行]

### チェック結果
- [x] 構文チェック合格
- [x] 表示チェック合格
- [x] データチェック合格

### 変更詳細
1. [変更1の説明]
2. [変更2の説明]

### 発見した派生課題
- （あれば記載。なければ「なし」）

### 次スレッドへの申し送り
- 次: Phase 5-[Y]
- 対象: [次のセクション名]
- 注意点: [あれば]
```

---

## 📊 Phase 5-1: Employment 詳細計画

### セクション構造（最も複雑）

```
行57-193: Employment セクション全体
├── 行57-58: セクションヘッダー「### Employment」
├── 行59-60: col1, col2 = st.columns(2)
│
├── col1 (左カラム) 行61-116:
│   ├── 行62-78: NFP（雇用統計）
│   ├── 行79-80: st.markdown("---") [内部区切り]
│   ├── 行81-95: ADP雇用統計
│   ├── 行96-97: st.markdown("---") [内部区切り]
│   └── 行98-116: 失業率
│
└── col2 (右カラム) 行117-193:
    ├── 行118-134: 平均時給
    ├── 行135-136: st.markdown("---") [内部区切り]
    ├── 行137-154: JOLTS
    ├── 行155-156: st.markdown("---") [内部区切り]
    └── 行157-193: ICSA（失業保険申請）
```

### 注意点
- **内部の `st.markdown("---")` はそのまま維持**
- セクション全体を1つのexpanderで囲む
- 内部の区切り線はexpander内に含める

### 変更パターン
```python
# Before
st.markdown(f"### {t('us_economic_section_employment')}")
col1, col2 = st.columns(2)

# After  
with st.expander(t('us_economic_section_employment'), expanded=True):
    col1, col2 = st.columns(2)
```

---

## 📊 Phase 5-2: Inflation + Economy 詳細計画

### Inflation セクション（行196-221）
- シンプルな2カラム構造
- `display_macro_card` を4回呼び出し
- 変更パターンA使用

### Economy セクション（行223-273）
- 2カラム構造
- 左: RetailSales, ConsumerSentiment
- 右: GDP, Yield Curve
- 変更パターンA使用

---

## 📊 Phase 5-3: Michigan + Leading 詳細計画

### Michigan Inflation（行275-295）
- 1:2のカラム比率
- Plotlyチャート含む
- 変更パターンA使用

### Leading Indicators（行497-515）
- シンプルな構造
- CFNAI指数のみ
- 変更パターンA使用

---

## 📊 Phase 5-4: Manufacturing 詳細計画

### 構造（行297-395）
```
├── セクションヘッダー + caption
├── 2x2グリッド（4つの地域連銀指数）
│   ├── Empire State
│   ├── Philly Fed
│   ├── Dallas Fed
│   └── Richmond Fed（特殊：get_richmond_fed_survey()使用）
└── 内部expander: manufacturing_guide（そのまま維持）
```

### 重要注意
- **内部の `with st.expander(t('manufacturing_guide')...` はネストとして維持**
- Richmond Fed の特殊データ取得ロジックは触らない

---

## 📊 Phase 5-5: Services + Housing 詳細計画

### Services セクション（行397-495）
- Manufacturing とほぼ同じ構造
- `get_richmond_fed_services_survey()` 使用
- 内部expander: `services_guide` 維持

### Housing セクション（行517-542）
- シンプルな2カラム
- Housing_Starts, Building_Permits
- 変更パターンA使用

---

## 🔗 関連ドキュメント

| ドキュメント | 用途 |
|-------------|------|
| `MOBILE_SCROLL_FIX_CHARTER.md` | 元の作業憲章（参考） |
| `PROJECT_RULES.md` | プロジェクト全体ルール |
| `pages/03_us_economic.py` | 作業対象ファイル |

---

## 📜 変更履歴

| 日付 | 変更内容 |
|------|----------|
| 2026-01-30 | 初版作成（v2.0 強化版） |

---

## ⚠️ 最終確認事項

この作業憲章を読んだ後、以下を確認してください：

1. **絶対禁止事項を理解した**
2. **変更パターンを理解した**
3. **チェックリストの意味を理解した**
4. **ロールバック手順を控えた**

理解できない点があれば、**作業を開始しないでください**。
質問してください。

---

Created: 2026-01-30
Author: Claude
Version: 2.0 (Strict Edition)
