# MARKET VERDICT Phase 5 - マルチアセットVERDICT実装ガイド
> 次スレッド引き継ぎ用ドキュメント
> 作成日: 2026-01-24
> 最終更新: 2026-01-24 (Step 1完了)
> 前スレッド: https://claude.ai/chat/777b13e8-7733-401d-9451-463b05af1093

---

## 🎯 Phase 5のゴール

**現状**: VERDICTは株式（S&P500）中心の判定のみ

**目標**: 株式・ゴールド・BTCの3資産クラスそれぞれにスコアと判定を出す

```
┌─────────────────────────────────────────────────────────┐
│        MARKET VERDICT（マルチアセット版）                │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  📈 株式:    やや強気 (65)  → シクリカル有利            │
│  🥇 ゴールド: 中立 (52)    → 様子見                    │
│  ₿ BTC:     強気 (78)     → 流動性追い風              │
│                                                         │
│  💡 今の環境では: BTC > 株式 > ゴールド                 │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

---

## ✅ 完了状況

### Phase 4（WHYセクション）✅ 完了
- `utils/verdict_why.py` を作成・更新
- 4人の投資家視点を反映
- センチメント解説のロジック修正
- 動的アクション示唆を実装

### Phase 5 Step 1 ✅ 完了 (2026-01-24)
- **`utils/verdict_assets.py` 新規作成**
  - `calculate_stock_verdict()` - 株式VERDICT（既存verdict_main流用）
  - `calculate_gold_verdict()` - ゴールドVERDICT（実質金利・VIX・テクニカル・DXY）
  - `calculate_btc_verdict()` - BTC VERDICT（流動性・テクニカル・DXY）
  - `calculate_multi_asset_verdict()` - 3資産統合＋ランキング
- **実質金利は既存データから計算**
  - `US_TNX` (DGS10) - `Breakeven_10Y` (T10YIE) = 実質金利
  - DFII10の追加は不要！

### Phase 5 残りステップ ⏳ 未着手
- [ ] Step 2: UI更新 (`pages/13_verdict.py`)
- [ ] Step 3: i18n対応 (`utils/i18n.py`)
- [ ] Step 4: Crypto Fear & Greed API対応（オプション）

---

## 📚 設計哲学（4人の巨人）

### 各資産クラスへの適用ロジック

| 投資家 | 株式への影響 | ゴールドへの影響 | BTCへの影響 |
|--------|-------------|-----------------|-------------|
| Druckenmiller | 流動性↑ → 株↑ | 流動性↑ → 金↓（リスクオン時） | 流動性↑ → BTC↑↑ |
| Howell | Net Liq↑ → 株↑ | Net Liq↑ → 金↓ | Net Liq↑ → BTC↑ (相関強い) |
| Dalio | 拡大期 → 株↑ | 冬/不確実性 → 金↑ | リスクオン → BTC↑ |
| Marks | 恐怖 → 株買い場 | 恐怖 → 金↑（安全資産） | 恐怖 → BTC↓（リスク資産） |

---

## 📊 各資産のスコア構成（実装済み）

### 株式スコア (verdict_main.py流用)
```python
STOCK_WEIGHTS = {
    'liquidity': 0.35,    # 流動性
    'cycle': 0.25,        # サイクル
    'technical': 0.25,    # テクニカル（S&P500）
    'sentiment': 0.15,    # センチメント
}
```

### ゴールドスコア (verdict_assets.py)
```python
GOLD_WEIGHTS = {
    'real_rate': 0.40,    # 実質金利（逆相関）← 最重要！
    'uncertainty': 0.25,  # VIX/不確実性
    'technical': 0.20,    # ゴールドのMA/RSI
    'dxy': 0.15,          # ドル指数（逆相関）
}
```

### BTCスコア (verdict_assets.py)
```python
BTC_WEIGHTS = {
    'liquidity': 0.45,    # 流動性が最重要（Howell）
    'technical': 0.25,    # BTCのMA/RSI
    'crypto_sentiment': 0.20,  # Crypto Fear & Greed（TODO）
    'dxy': 0.10,          # ドル指数（逆相関）
}
```

---

## 🗂️ 関連ファイル一覧

| ファイル | 役割 | Phase 5での状態 |
|---------|------|----------------|
| `utils/verdict_main.py` | 株式スコア計算 | 変更なし |
| `utils/verdict_liquidity.py` | 流動性スコア | 変更なし |
| `utils/verdict_cycle.py` | サイクルスコア | 変更なし |
| `utils/verdict_technical.py` | テクニカルスコア | 変更なし（Gold/BTC用に再利用） |
| `utils/verdict_sentiment.py` | センチメントスコア | 変更なし |
| `utils/verdict_why.py` | WHY生成 | 変更なし（将来: 3資産対応追加） |
| **`utils/verdict_assets.py`** | **3資産スコア** | **✅ 新規作成済み** |
| `pages/13_verdict.py` | UI表示 | ⏳ 次ステップで3ゲージ追加 |
| `utils/i18n.py` | 翻訳キー | ⏳ 次ステップで追加 |

---

## 📋 次スレッドでの作業チェックリスト

### Step 2: UI更新 (pages/13_verdict.py)

```python
# 追加すべきコード概要

# 1. インポート追加
from utils.verdict_assets import calculate_multi_asset_verdict

# 2. データ準備関数の拡張
def prepare_multi_asset_data(df: pd.DataFrame) -> dict:
    """マルチアセット用にGold, BTC, DXY等を追加"""
    data = prepare_verdict_data(df)  # 既存関数
    
    # 追加データ
    if 'Gold' in df.columns:
        data['Gold'] = df['Gold'].dropna()
    if 'BTC' in df.columns:
        data['BTC'] = df['BTC'].dropna()
    if 'DXY' in df.columns:
        data['DXY'] = df['DXY'].dropna()
    if 'US_TNX' in df.columns:
        data['US_TNX'] = df['US_TNX'].dropna()
    if 'Breakeven_10Y' in df.columns:
        data['Breakeven_10Y'] = df['Breakeven_10Y'].dropna()
    
    return data

# 3. 3資産ゲージ表示
def render_multi_asset_gauges(multi_verdict: dict):
    col1, col2, col3 = st.columns(3)
    
    with col1:
        render_asset_gauge(multi_verdict['stock'])
    with col2:
        render_asset_gauge(multi_verdict['gold'])
    with col3:
        render_asset_gauge(multi_verdict['btc'])

# 4. ランキング表示
st.subheader("💡 現環境での優先度")
st.info(multi_verdict['recommendation'])
```

### Step 3: i18n対応

`utils/i18n.py` に追加が必要なキー:
```python
# 資産ラベル
'verdict_asset_stock': {'en': '📈 Stocks', 'ja': '📈 株式'},
'verdict_asset_gold': {'en': '🥇 Gold', 'ja': '🥇 ゴールド'},
'verdict_asset_btc': {'en': '₿ Bitcoin', 'ja': '₿ ビットコイン'},

# 柱ラベル
'verdict_pillar_real_rate': {'en': 'Real Rate', 'ja': '実質金利'},
'verdict_pillar_uncertainty': {'en': 'Uncertainty', 'ja': '不確実性'},
'verdict_pillar_dxy': {'en': 'USD Index', 'ja': 'ドル指数'},
'verdict_pillar_crypto_sentiment': {'en': 'Crypto Sentiment', 'ja': '暗号資産センチメント'},

# ランキング
'verdict_ranking_title': {'en': 'Current Priority', 'ja': '現環境での優先度'},
```

### Step 4: Crypto Fear & Greed API（オプション）

```python
# alternative.me API
import requests

def fetch_crypto_fear_greed():
    url = "https://api.alternative.me/fng/?limit=1"
    response = requests.get(url, timeout=10)
    data = response.json()
    return int(data['data'][0]['value'])  # 0-100
```

---

## ⚠️ 注意点

### 実装時の留意事項

1. **実質金利データ**
   - 既存データから計算: `US_TNX - Breakeven_10Y`
   - DFII10の追加は不要

2. **ゴールドの複雑性**
   - 単純に流動性↑→ゴールド↑ではない
   - インフレ期待、実質金利、ドル、地政学リスクの複合要因
   - 「安全資産」と「インフレヘッジ」の二面性

3. **BTCの特殊性**
   - 流動性との相関が非常に強い（Howell研究）
   - 半減期サイクル（4年周期）の影響
   - Crypto市場固有のセンチメント指標が有効

4. **免責表示の強化**
   - 3資産に拡大するため、投資助言と誤解されないよう注意

5. **既存UIへの影響なし**
   - 現在の株式VERDICTはそのまま動作
   - 3資産ゲージは追加表示として実装

---

## 📜 スレッド履歴

| # | 日付 | URL | 内容 |
|---|------|-----|------|
| 1 | 01-23 | https://claude.ai/chat/ee4ad37a-e43c-437f-8b20-bf9d0f0b1121 | 構想・設計哲学確定 |
| 2-3 | 01-23 | (Project内) | Phase 1-2.6 実装 |
| 5 | 01-23 | https://claude.ai/chat/3f892a96-3a63-45f0-a7b6-c880938d12f1 | Phase 3 UI完成 |
| 6 | 01-24 | https://claude.ai/chat/777b13e8-7733-401d-9451-463b05af1093 | Phase 4 WHY完成 |
| 7 | 01-24 | https://claude.ai/chat/848870fa-b0be-47e3-908b-6737efc1a07b | Phase 5 Step 1: verdict_assets.py作成 |

---

## 🚀 次スレッドへの指示

```
MARKET VERDICT Phase 5 Step 2（UI更新）を進めたいです。

📄 引き継ぎファイル: HANDOFF_VERDICT_PHASE5.md
📁 場所: C:\Users\81802\.gemini\antigravity\scratch\market_monitor\

完了済み:
- ✅ utils/verdict_assets.py 新規作成
- ✅ 3資産スコア計算ロジック実装

次のステップ:
- pages/13_verdict.py のUI更新
- 3資産ゲージを横並び表示
- ランキング表示追加

注意:
- 既存UIに影響を与えない追加実装
- 超慎重モードで作業

前スレッド: https://claude.ai/chat/848870fa-b0be-47e3-908b-6737efc1a07b
```

---

**質問があれば次のスレッドで聞いてください！** 🚀
